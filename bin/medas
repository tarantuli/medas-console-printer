#!/usr/bin/env php
<?php

use Medas\ConsolePrinter\CommandProcessor;

require_once __DIR__ . '/../bootstrap.php';

$arguments = $argv;

if (str_ends_with($arguments[0], 'medas')) {
    array_shift($arguments);
}

service(CommandProcessor::class)->process($arguments);
